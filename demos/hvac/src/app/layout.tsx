import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Climate Comfort HVAC | Year-Round Comfort, Just One Call Away",
  description: "Professional HVAC services in Charlotte, NC. NATE certified technicians offering AC repair, heating, duct cleaning, and indoor air quality. 24/7 emergency service available!",
  keywords: "hvac charlotte nc, ac repair charlotte, heating repair charlotte, hvac contractor, emergency hvac, nate certified",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
