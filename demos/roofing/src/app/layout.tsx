import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Summit Roofing Co. | Protecting Charlotte Homes From The Top Down",
  description: "Premium roofing services in Charlotte, NC. GAF certified contractors offering roof repair, replacement, inspection, and emergency storm damage services. Get your free quote today!",
  keywords: "roofing charlotte nc, roof repair charlotte, roof replacement, roofing contractor, storm damage roofing, summit roofing",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
